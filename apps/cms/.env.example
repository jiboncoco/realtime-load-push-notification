# Base URL backend API (dev). Caddy akan menyatukan origin saat produksi.
NEXT_PUBLIC_API_BASE=http://localhost:3001

# Base URL customer web app — dipakai QR code outlet (mengarah ke /o/{id}).
NEXT_PUBLIC_CUSTOMER_BASE=http://localhost:3002
